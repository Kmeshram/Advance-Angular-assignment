import { OnhovertextcolorDirective } from './onhovertextcolor.directive';

describe('OnhovertextcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new OnhovertextcolorDirective();
    expect(directive).toBeTruthy();
  });
});
